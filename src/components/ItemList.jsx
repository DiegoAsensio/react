import React from 'react'
import Item from '../Item';

/*const ItemList = ({resultado}) => {
  return (
    <div>
      {resultado?.map(resultado => <Item />)}
    </div>
  )
}

export default ItemList*/